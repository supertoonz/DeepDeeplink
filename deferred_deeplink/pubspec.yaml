name: deferred_deeplink
description: "A standalone Flutter package for custom deferred deep linking."
version: 0.1.0
publish_to: 'none'
homepage: 'https://example.com'

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  device_info_plus: ^9.0.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
